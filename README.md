# RAG_API_w_FastAPI
This project implements a modular RAG pipeline that allows developers to query documents, retrieve the most relevant context, and generate high‑quality answers through an LLM. It exposes clean, well‑structured API endpoints using FastAPI, making it easy to integrate into applications, dashboards, or automation workflows.
